


<h4><%= @quote.text %></h4>
Author: <%= @quote.author.name %>

<%= link_to "Edit", edit_quote_path(@quote), class: "btn btn-danger" %>

<% @quote_concepts.each do |concept| %>
  <span class="btn btn-primary"><%= concept.name %></span>
<% end %>
